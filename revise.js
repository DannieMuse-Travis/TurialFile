

const prompt = require("prompt-sync")();




    // Math.floor(Math.random()=(max-min))+min


    // filter is one of the most used methode
    // strength will rise when we wait just
    // on the lord,so we will wait
    // just on the lord
    let name= prompt("Enter your name")
    console.log(name);